export interface Template {
  name: string; // The name of the template, e.g., "Community DAO"
  description: string; // A short description explaining the template's purpose
  imageUrl: string; // URL to an image representing the template
}
